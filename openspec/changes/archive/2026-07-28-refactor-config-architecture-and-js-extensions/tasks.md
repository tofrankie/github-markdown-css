## 1. 配置模型重构

- [x] 1.1 设计并落地集中配置目录，拆分项目级配置、token 来源 registry、主题元数据和发布矩阵
- [x] 1.2 将现有 `build-config` 中的来源路径、主题说明和 `auto*` 组合迁移到新的声明式配置模块
- [x] 1.3 为来源配置对象补齐最小必要字段与用途说明，确保新增来源时默认只改 registry

## 2. 构建脚本分层

- [x] 2.1 按流水线阶段重组脚本模块，拆分配置加载、来源收集、token scope 解析、产物生成、验证和写盘职责
- [x] 2.2 调整顶层构建入口以消费新的配置模块，并保持现有 `dist/` 与 `artifacts/` 输出语义稳定
- [x] 2.3 清理旧的隐式 hook 组织方式，确保发布矩阵与来源 registry 不再散落在多个工具模块里

## 3. 文档重写

- [x] 3.1 重写 `artifacts/README.md`，按“全局流程 -> 核心概念 -> 开发/构建/验证 -> 如何新增配置 -> 注意事项 -> 实现细节”的顺序组织内容
- [x] 3.2 校对根 `README.md` 的用户向定位，必要时同步更新与维护者文档相关的链接或简短说明
- [x] 3.3 在维护者文档中明确新增来源、扩展 `auto*` 组合和扩展 markdown 入口各自需要修改的文件与验证步骤

## 4. ESM `.js` 扩展名统一

- [x] 4.1 将仓库内 Node 构建与验证脚本从 `.mjs` 统一迁移为 `.js`
- [x] 4.2 同步更新脚本间 import、`package.json` scripts、文档示例和其他所有 `.mjs` 引用
- [x] 4.3 通过全仓检索确认不再残留旧的 `.mjs` 路径引用

## 5. 验证与收尾

- [x] 5.1 运行构建与验证命令，确认 slim/full/published/report 链路和对外 CSS 产物保持可用
- [x] 5.2 复核新增来源、主题元数据和发布矩阵的维护路径是否满足“默认改一个声明入口即可”的目标
- [x] 5.3 根据最终实现更新 OpenSpec 任务状态与相关说明，确保变更已准备好进入归档或后续实现迭代
